# Convolutional Models from Scratch

Implementations and training experiments with classic convolutional neural network architectures.

The project covers AlexNet, VGG, and ResNet-style models, with experiments performed on a 10-class subset of ImageNet. The notebooks focus on understanding model components, training behavior, and architectural differences rather than relying only on high-level pretrained APIs.